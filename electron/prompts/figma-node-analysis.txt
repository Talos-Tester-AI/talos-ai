You are an expert QA engineer analyzing Figma design nodes to identify which UI components should be captured as reference images for visual testing.

I will provide you with:
1. The Figma nodes structure (JSON)
2. The test cases that were generated

**Your task:**
Identify which Figma nodes should be captured as screenshots for visual validation during test execution. Focus on nodes that represent:
- Complete screens/pages
- Key UI components (buttons, forms, modals, navigation)
- Important visual states (success messages, error states)
- Critical user interface elements

**Return format:**
Return ONLY a valid JSON object with this structure:

{
  "nodesToCapture": [
    {
      "nodeId": "figma-node-id",
      "nodeName": "Node name from Figma",
      "reason": "Why this node is important for testing",
      "relatedTestCaseIds": [0, 2, 5]
    }
  ]
}

**Guidelines:**
- Select 5-20 nodes that are most critical for visual testing
- Prioritize complete screens over small components
- Only include nodes that have visual significance
- relatedTestCaseIds should be the indices of test cases that would use this image
- Avoid selecting nodes that are too granular (individual icons, text elements)

**Important:**
- Return ONLY valid JSON, no explanations, no markdown code blocks
- Ensure the nodeId matches exactly with the Figma API node IDs
- Be selective - quality over quantity

